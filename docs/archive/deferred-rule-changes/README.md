# Deferred rule changes

Shipped during Phase 1. A change that permits more app use waits for the next daily reset instead of applying at once, so the allowance cannot be edited around within a day.

- [design.md](design.md) — the model, the reset event, and how a save is routed.
- [plan.md](plan.md) — the implementation plan, frozen as executed.

Superseded in part by [per-app rule changes](../per-app-rule-changes/), which replaced the whole-document judgment with one judgment per app.
